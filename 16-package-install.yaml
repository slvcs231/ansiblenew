- name: install packages
  hosts:  frontend
  tasks:
  - name: install packages
    ansible.builtin.package:
      name: {{ item }}
      state:  installed
    loop:
    - nginx
    - git 
    - wget
    - mysql